# 复制本文件为 .env.development(本地开发用)和 .env.production(构建/线上用),
# 并填入你自己的 UUID(python -c "import uuid; print(uuid.uuid4())")。
# 两个文件内含详细的启动/访问说明。生产密钥还需同步配置到 Cloudflare Pages 环境变量。
VITE_EDIT_KEY=00000000-0000-0000-0000-000000000000
