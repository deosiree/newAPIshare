# 验收报告 v2（React 架构升级,2026-08-30）

## 交付总览(三批次,均已推送)

| 批次 | 内容 | 提交 | 状态 |
|---|---|---|---|
| 批1 | Vite+React19+TS 脚手架;数据源 data.js → `public/sites.csv`(UTF-8 BOM)+`public/columns.json`;浏览态 React 化(注册/签到按钮缩小合并到站名下方,信息密度优化);`.env` UUID 密钥+访问注释;全部 Python 工具迁移 CSV | `24373fc` | ✅ |
| 批2 | `/edit` Excel 式编辑态:AG Grid Community(MIT)+SheetJS;工具栏(保存/撤销/重做/导入导出Excel/超链接/±行列/居中/靠左/快速过滤/显示列/同步New API);撤销重做快照栈;右键隐藏行列/删行列;列宽列序持久化;`/save` 全量保存端点 | `7a27807` | ✅ |
| 批3 | 文档全量重写(README+部署向导:含 Pages 构建切换/更新/回滚/域名/排障)+本报告 | 本次 | ✅ |

## 需求对照

| 需求 | 落地 |
|---|---|
| 1. 改成 React | Vite6+React19+TS,CF Pages 构建 `npm run build`→`dist` |
| 2. .env UUID 密钥+访问注释 | `.env`/`.env.example`;`/?k=UUID` 解锁私列,`/edit` 编辑态,`?k=off` 退出;⚠️ Pages 环境变量需同步配置 |
| 3. Excel 式编辑态 | 工具栏全部按钮落地;单元格直编;列宽/列序持久化;整行整列选择+右键隐藏(浏览态不可见);快速过滤(状态/评分) |
| 4. 优先用成熟项目 | 调研后选定 AG Grid Community(MIT)+SheetJS(Apache-2.0);Univer/FortuneSheet/Handsontable 因映射成本/停更/商用授权被否,见对话记录 |

## 测试结果

- 数据断言 14/14(CSV BOM、行数、评价列计数、free 误配回归、哈基米一致、签到同主机)
- 同步全链路 **36/36**(API+DOM 抓取、/apply 差量写入、**/save 全量保存**、现场复位)
- 构建:`tsc --noEmit` + vite build 通过;产物含 index.html/sites.csv/columns.json
- 截图冒烟:浏览公开态/私人视图(私列+站名下双按钮)/编辑态(深色网格+全工具栏)

## 已知限制与说明

1. **线上切换需要你动手**:Pages 构建设置改为 `npm run build`/`dist` + 环境变量(见部署向导最上方「一次性迁移」,2 分钟)——在此之前线上白屏是预期的
2. 前端包体 1.78MB(gzip 532KB,AG Grid+SheetJS);浏览态已按需加载,后续可做 /edit 路由分包
3. 撤销栈为会话级(保存后清空),跨会话回滚用 Deployments 回滚
4. CSV 含私列数据,公开可下载——与原 data.js 同语义;绝密内容仍需保持离线
5. `newapishare.pages.dev` 此前 DNS 查询不存在,确认你的 Pages 项目实际网址后替换文档占位

## 待人工清单

1. **Pages 构建设置切换**(见上,必做,否则线上白屏)
2. 换掉/确认 `VITE_EDIT_KEY`(当前 cbe72d15-…,建议重生成一个)
3. 审查 `/edit` 编辑态交互(列宽拖拽/右键菜单/撤销),不满意的地方列入下轮
4. (可选)免费/付费域名绑定
